document.addEventListener('DOMContentLoaded', function() {
    // If we're on the input form page
    const cropInputForm = document.getElementById('crop-input-form');
    if (cropInputForm) {
        // Let the form submit normally (no prevent default)
        // This will do a regular form POST instead of JSON
    }
});